// Copy this file to Secrets.xcconfig for local-only values.
// Secrets.xcconfig is ignored by Git. Never add provider credentials to an
// application target: live meal analysis must use a server-side proxy.
PROTEIN_PROXY_BASE_URL = https://example.invalid
