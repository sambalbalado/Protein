{
  "name": "protein-meal-analysis-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --test worker.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
